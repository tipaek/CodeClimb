{
  "name": "codeclimb-frontend",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "echo 'dev placeholder: run npm install and wire Vite when dependency access is available'",
    "build": "echo 'build placeholder'",
    "lint": "echo 'lint placeholder: no lint rules configured yet'",
    "typecheck": "echo 'typecheck placeholder: no TS compiler configured yet'",
    "test": "echo 'test placeholder: no frontend tests configured yet'"
  }
}
